{
  "name": "fernando-lucoco-music-web",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Local-first vocal studio web experience by Fernando Lucoco.",
  "scripts": {
    "test": "node --test $(find tests -maxdepth 1 -name '*.test.mjs' ! -name '*.vitest.test.mjs' -print)"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "vitest": "^4.1.10"
  }
}
